MODELS_TASK_FLOW_CSS = """
    /*
     * Section headings
     */

    .cogsim-model-section-header {
        margin-bottom: 0.8rem;
    }

    .cogsim-model-section-title {
        margin-bottom: 0.2rem;

        color: var(--cogsim-text);
        font-size: 0.9rem;
        font-weight: 650;
        line-height: 1.35;
    }

    .cogsim-model-section-description {
        max-width: 720px;

        color: var(--cogsim-text-secondary);
        font-size: 0.74rem;
        line-height: 1.45;
    }

    .cogsim-model-subsection-title {
        margin-bottom: 0.15rem;

        color: var(--cogsim-text);
        font-size: 0.8rem;
        font-weight: 650;
        line-height: 1.35;
    }

    .cogsim-model-subsection-copy {
        margin-bottom: 0.65rem;

        color: var(--cogsim-text-secondary);
        font-size: 0.7rem;
        line-height: 1.4;
    }

    [class*="st-key-edit_dimensions_from_"] button {
        display: inline-flex;
        min-width: 2.65rem;
        width: 2.65rem;
        height: 2.65rem;
        min-height: 2.65rem;
        align-items: center;
        justify-content: center;

        padding: 0;

        border-radius: 999px;

        font-size: 1rem;
        font-weight: 720;
        white-space: nowrap;
        box-shadow: 0 8px 22px rgba(17, 24, 39, 0.055);
    }

    [class*="st-key-edit_dimensions_from_"] button svg {
        width: 1rem;
        height: 1rem;
    }

"""
